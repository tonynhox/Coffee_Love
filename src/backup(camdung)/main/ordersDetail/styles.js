import { StyleSheet, Text, View } from 'react-native'


export const styles = StyleSheet.create({
    borderView:{
        borderWidth:0.3,borderColor:'#000',marginHorizontal:10
    },
    container:{
        // flex:1,
        flexDirection:'column',
        paddingHorizontal:6,
        backgroundColor:'#fff',
        marginVertical:10,
        // marginHorizontal:10,
    },
})